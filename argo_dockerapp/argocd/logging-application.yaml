# Place alongside k8s/argocd/app/kyc-python-application.yaml in your repo.
# Adjust repoURL/path to wherever you commit the logging/k8s/ folder above.
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: kyc-logging-stack
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/BunlongCHEA/Terraform-Plan.git
    targetRevision: HEAD
    path: argo_dockerapp/elk-fluentbit/k8s
    directory:
      recurse: true
  destination:
    server: https://kubernetes.default.svc
    namespace: logging
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true   # in case the 00-namespace.yaml hasn't landed yet
